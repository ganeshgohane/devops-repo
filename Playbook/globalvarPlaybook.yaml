- name: Playbook for globalvariable
  hosts: all
  vars:
        URL: "www.google.com"
  tasks:
        - name: print hello world
          debug:
          msg: "Please visit {{URL}}"

        - name: print hello world
          debug:
          msg: "Please visit {{URL}}"

